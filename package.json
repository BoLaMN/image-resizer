{
  "name": "image-resizer",
  "version": "1.0.1",
  "description": "image resizing and optimization using node and sharp (libvips).",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "preinstall": "#mkdir -p ~/.npm/_libvips/ && cp libvips/libvips-8.10.6-linux-x64.tar.br ~/.npm/_libvips/",
    "compile": "coffee --bare --no-header --output dist/ --compile src/"
  },
  "author": {
    "name": "BoLaMN",
    "url": "https://github.com/BoLaMN"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/BoLaMN/image-resizer"
  },
  "license": "MIT",
  "dependencies": {
    "connect": "^3.7.0",
    "methods": "^1.1.2",
    "sharp": "^0.28.1"
  }
}
